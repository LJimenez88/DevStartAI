# Example environment variables for the generated FastAPI project

# Used by SQLAlchemy / your DB layer
DATABASE_URL=postgresql://user:password@localhost:5432/app_db

# When running with docker-compose, the app container will use:
# postgresql://user:password@db:5432/app_db
