# Example environment variables for the generated FastAPI project
# Copy this file to `.env` and adjust values as needed.

# ---------------------------
# Database configuration
# ---------------------------
# For PostgreSQL (local dev):
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/app_db

# For PostgreSQL (Docker compose):
# DATABASE_URL=postgresql+psycopg2://app_user:app_password@postgres-db:5432/app_db

# For SQLite (no external DB needed):
# DATABASE_URL=sqlite+aiosqlite:///./app.db

# ---------------------------
# Application settings
# ---------------------------
APP_ENV=development

# Generic DB URL (used by MySQL addon)
DATABASE_URL=mysql+pymysql://app_user:app_password@mysql-db:3306/app_db

# For docker-mysql service
MYSQL_DATABASE=app_db
MYSQL_USER=app_user
MYSQL_PASSWORD=app_password
MYSQL_ROOT_PASSWORD=rootpassword
